#include <stdio.h>
#include <string.h>

void inverse_bits ( unsigned int bitmap ) {
  
    
}
int main(void) {
  char src[50], dest[50];

   strcpy(src,  "This is source");
   strcpy(dest, "This is destination");

   str(dest, src);

   printf("Final destination string : |%s|", dest);
   
   return(0);
}